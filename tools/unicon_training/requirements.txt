# UniCon Training Pipeline Dependencies
#
# Install: pip install -r tools/unicon_training/requirements.txt
#
# For GPU training (recommended): install PyTorch with CUDA support first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
numpy>=1.24.0
mujoco>=3.0.0
